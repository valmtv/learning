#include <stdio.h>
#include <stlib.h> // for rand() and others
#include <time.h>  // to set up srand(), use clock() ...
#include <math.h>  // use math functions: pow(), sqrt(), atan()...


int main(void) {

  return 0;
}

